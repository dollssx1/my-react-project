export default function BaseInput(props: any) {
    const title = props.title;

    const test= (e: any)=>{
        console.log('ㅇ아앙아')
    };

    return (
        <input type="text" />
    )
  }
  
